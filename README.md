# Markup: type-safe HTML, XML, RSS generation using Swift

## Getting Started

Add the following dependency clause to your Package.swift:

```swift
dependencies: [
    .package(url: "https://github.com/sonaelabs/markup.git", from: "1.0.0")
]
```
